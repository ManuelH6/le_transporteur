// lib/core/app_theme.dart
// Extended theme colors used across the app.
// For the main ThemeData, use AppTheme.theme from core/theme/app_theme.dart.

import 'package:flutter/material.dart';

class AppTheme {
  // Primary brand color (orange-red)
  static const Color primaryColor = Color(0xFFFF491D);

  // Secondary brand color (yellow)
  static const Color secondaryColor = Color(0xFFFEBE05);

  // Text colors
  static const Color textPrimary = Color(0xFF333333);
  static const Color textSecondary = Color(0xFF888888);

  // Status colors
  static const Color successColor = Color(0xFF4CAF50);
  static const Color errorColor = Color(0xFFF44336);
  static const Color warningColor = Color(0xFFFF9800);

  // Background
  static const Color backgroundColor = Color(0xFFFFF5F0);
}
